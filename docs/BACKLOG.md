# BACKLOG — Gap Ditemukan Selama Eksekusi

> Per SSOT §9.4: gap baru yang ditemukan saat eksekusi dicatat di sini (tidak dikerjakan di luar urutan kecuali P0/blocker). Ditinjau saat perencanaan fase.

| ID | Temuan | Ditemukan saat | Severity | Rekomendasi | Target Fase |
|----|--------|----------------|----------|-------------|-------------|
| **EPIC 8 — Content Integrity (audit live jagoakademi.com, 2 Jul 2026)** | | | | | |
| BL-23 | **Statistik fiktif tayang di produksi**: Hero "50K+ Pelajar/200+ Kursus/4.9 rating" + trust "500+ trainer/50+ perusahaan/#1 Indonesia"; about "50.000+/500+/200+"; early-access "500+/50+"; e-course "1.000+ Skill". DB nyata KOSONG → menyesatkan + risiko PDP/hukum. | Audit live | 🔴 Blocker | TASK-052: angka dari API real; jika 0 → hide/empty-state/copy netral. | Pre-Soft-Launch |
| BL-24 | **Testimoni & klien bernama tanpa consent**: home/e-course TestimonialsSection, clients page (Tokopedia/Astra/dll), nama orang (Rizky/Sari Dewi/Budi Santoso). | Audit live | 🔴 Blocker | TASK-052: empty-state "Segera hadir" sampai TASK-095 (testimoni real termoderasi). | Pre-Soft-Launch |
| BL-25 | **Leaderboard XP fiktif** (Budi Santoso 2.450 XP dll) + gamifikasi TIDAK ada di blueprint. | Audit live | 🔴 Blocker | ✅ **RESOLVED → TASK-097 (Phase 6)**. Keputusan reviewer final (2 Jul 2026): gamifikasi/XP disetujui jadi task resmi TASK-097; SSOT diperbarui (TD-27 + TASK-097). `LeaderboardSection.tsx` DIPERTAHANKAN (bukan dihapus) sebagai starting point, tidak di-import di manapun selama `features.gamification` OFF — data contoh di dalamnya harus diganti data real sebelum pernah dirender. | ✅ Done — scoped to TASK-097 |
| BL-26 | **Kursus contoh hardcoded** di `ECourseCatalog.tsx` (Ahmad Fauzi/Dewi Pratiwi dll, rating 4.9, students 12.5K) + `lib/e-course/data.ts` (studentCount 66.396 dll). | Audit live | 🔴 Blocker | TASK-052: tarik dari katalog real; kosong → empty-state. Data statis 3-level → hapus angka fiktif / gate. | Pre-Soft-Launch |
| BL-27 | **Link ke halaman belum jadi**: /marketplace (label "Baru"), /lms, /trainer-program, /afiliasi, /demo, /kolaborasi, /klien, /event-sebelumnya, /faq, /hubungi-kami. | Audit live | 🔴 High | TASK-053: feature flag OFF + gate link (hide / "Segera Hadir"). | Pre-Soft-Launch |
| BL-28 | **Klaim fitur belum ada di /e-course**: Learning Path (TASK-090), Community Lifetime (TASK-092), All-access subscription (TASK-093) — semua BELUM dibangun. | Audit live | 🟡 Medium | ✅ **RESOLVED (TASK-054)**: klaim Community/Lifetime & "Ratusan Ribu Member" diganti benefit riil; deep Learning-Path pages di-gate (`dynamicParams=false`, 404 saat flag OFF, static pages 149→54); CTA → early-access. | ✅ Done |
| BL-29 | **Auth pages perlu verifikasi**: /masuk, /daftar, /lupa-password (fetch tanpa JS kosong — konfirmasi client-component vs rusak) + E2E auth vs live. | Audit live | 🔴 High | ✅ **RESOLVED (TASK-055)**: semua client-component & berfungsi; login tidak butuh verifikasi email (tidak memblokir). E2E auth-flow 13/13 hijau (web-only preview). 3 temuan turunan → fix A+C (di bawah), B → BL-31. | ✅ Done |
| BL-30 | **Google OAuth callback rusak**: API redirect ke `${WEB_URL}/auth/callback?token=...` (auth.ts:363) tapi halaman web `/auth/callback` TIDAK ADA → 404, token hilang, CTA "Masuk/Daftar dengan Google" gagal senyap. | TASK-055 | 🔴 High | ✅ **Fix web (TASK-055)**: buat `/auth/callback` (konsumsi token → sessionStorage, scrub URL, redirect /dashboard; state error). **Sisi host masih perlu**: set `GOOGLE_CLIENT_ID/SECRET` + daftarkan redirect URI `https://jagoakademi.com/auth/callback` (dan `${API}/api/auth/google/callback`) di Google Cloud Console, lalu uji end-to-end di produksi. | Web ✅ / Host verify |
| BL-31 | **Email verifikasi tidak terkirim di produksi**: `POST /api/auth/register` membuat `emailVerifyToken` tapi hanya mengembalikan token di non-prod (auth.ts:114-115); prod TIDAK mengirim email & tidak ada halaman web konsumsi token. UI /daftar menjanjikan "tautan verifikasi telah dikirim" yang tidak pernah tiba. Tidak memblokir login (login tak cek verifikasi). | TASK-055 | 🟡 Medium | Kirim email verifikasi via queue (Resend/Fonnte, TASK-030) + halaman `/verifikasi-email?token=` yang panggil `verifyEmail()`. Sementara: copy /daftar sudah jujur (login tetap bisa). | Phase 5 (email infra) |
| BL-18 | **Tidak ada endpoint data-access/export** (hak akses PDP Art. 5-6). | TASK-051 | 🟡 Medium | Tambah `GET /api/users/me/export` (bundle JSON data user). | Phase 5 |
| BL-19 | **Tidak ada retention policy** — `AuditLog` (IP/UA) + akun teranonimisasi menumpuk tanpa batas (PDP Art. 43). | TASK-051 | 🟡 Medium | Retention policy + job purge `AuditLog` > 12 bln (BullMQ scheduled). | Phase 5 |
| BL-20 | **Consent tanpa versi kebijakan** — hanya `consentGivenAt`, tak bisa trigger re-consent saat policy berubah. | TASK-051 | 🔴 High (pre-launch) | Simpan `privacyPolicyVersion` saat consent. | Phase 5 (pre-launch) |
| BL-21 | **Breach-response process** (PDP Art. 46, 3×24 jam) — bagian ditambahkan ke RUNBOOK_INCIDENT (TASK-051), butuh formalisasi legal. | TASK-051 | 🔴 High (pre-launch) | Finalisasi workflow notifikasi 72 jam + kontak otoritas. | Phase 5 (pre-launch) |
| BL-22 | **Cross-border transfer belum di-assess** (PDP Art. 56) — Cloudflare R2/Stream, Resend, Sentry mungkin di luar Indonesia. | TASK-051 | 🔴 High (pre-launch) | Review DPA per processor + disclosure di privacy notice. | Phase 5 (pre-launch, legal) |
| BL-17 | **Web Sentry belum ada** — TASK-023 menambah observability API (Sentry+pino+/ready). Sisi web (Next.js) belum diinstrumentasi (butuh `@sentry/nextjs` + `instrumentation.ts` + sentry configs). | TASK-023 | 🟡 Medium | Install `@sentry/nextjs`, wire client/server/edge config + `NEXT_PUBLIC_SENTRY_DSN`, verifikasi build. | Phase 5 (pra public launch) |
| BL-01 | **5 apps di monorepo** (`admin`, `api`, `lms`, `trainer`, `web`), bukan 2. `apps/admin`, `apps/lms`, `apps/trainer` hanya scaffold `create-next-app` default (1 boilerplate `page.tsx` masing-masing). Fungsi asli admin/lms/trainer ada di dalam `apps/web`. | TASK-001 | 🟡 Medium | Hapus 3 scaffold mati atau putuskan repurpose. SSOT hanya mengakui `apps/api` + `apps/web`. Konfirmasi stakeholder. | Phase 2 (cleanup) |
| BL-02 | **52 type error API** (`trainer.ts` 21, `lms.ts` 14, dll) — referensi field/relasi Prisma yang tidak ada di schema. | TASK-000 | 🔴 High | Diselesaikan di **TASK-002**. | Phase 1 |
| BL-03 | **Bug runtime `lms.ts:288`** — `POST /api/lms/invite/:token/accept` → 500. | TASK-000 | 🔴 High | Fix + regression test di **TASK-002**. | Phase 1 |
| BL-04 | **API lint tak berjalan** — `eslint.config.js` (flat config v9) tidak ada di `apps/api`. | TASK-000 | 🟡 Medium | Tambah config referensi `@repo/eslint-config` di **TASK-002/004**. | Phase 1 |
| BL-05 | **Web lint diblok** — 24 warning `turbo/no-undeclared-env-vars` (env `CI` di `playwright.config.ts` tak dideklarasi `turbo.json`) vs `--max-warnings 0`. | TASK-000 | 🟢 Low | Deklarasikan `CI` di `turbo.json` `globalEnv` atau sesuaikan. **TASK-002/004**. | Phase 1 |
| BL-06 | **Coverage % belum terukur** — klaim 60% tak berbukti. | TASK-000 | 🟡 Medium | Ukur resmi di **TASK-010** (`vitest --coverage`). | Phase 2 |
| BL-07 | **docs & report belum di-push** — commit lokal saja (per instruksi reviewer: jangan push tanpa konfirmasi). | TASK-001 | 🟢 Low | Push setelah konfirmasi reviewer. | Phase 1 |
| BL-08 | **13 `<img>` elemen** memicu `@next/next/no-img-element`. Rule di-disable sementara di `apps/web/eslint.config.js` karena banyak `<img>` merender URL user arbitrer (avatar, thumbnail, cover) yang akan rusak dengan next/image domain allowlist. | TASK-002 | 🟡 Medium | Migrasi ke next/image dengan custom CDN loader (mendukung URL arbitrer), lalu re-enable rule. | Phase 5 (TASK-060 performance) |
| BL-09 | **`console.error` di `apps/web/app/error.tsx`** (client error boundary) — melanggar "no console in production" (§9.5). | TASK-002 | 🟢 Low | Ganti dengan Sentry client capture saat observability aktif. | Phase 3 (TASK-023) |
| BL-10 | **`tsconfig.test.json` melonggarkan `noUncheckedIndexedAccess`** untuk scaffolding mock. Produksi tetap strict. | TASK-002 | 🟢 Low | Pertimbangkan helper mock typed (deep-mock) agar bisa strict kembali. | Phase 2 (TASK-010) |
| BL-12 | **Zod belum di semua boundary query/params** — TASK-011 menstandarkan envelope + validasi body (18/26 route) + guard params. Validasi Zod menyeluruh untuk `req.query`/`req.params` di semua endpoint belum lengkap. Helper `parsePageParams` tersedia tapi belum diadopsi semua route. | TASK-011 | 🟡 Medium | Tambah Zod schema query/params per route secara bertahap; adopsi `asyncHandler`+`parsePageParams`. | Phase 2+ (incremental) |
| BL-13 | **Envelope belum di `packages/types`** — TASK-011 (Option B) menempatkan envelope otoritatif di `apps/api/src/types` + mirror tipe di `apps/web/lib/auth`. Konsolidasi ke `@repo/types` sebagai SSOT ditunda (butuh wiring build workspace). | TASK-011 | 🟢 Low | Pindahkan `ApiResponse`/`ApiError`/`PaginationMeta` ke `@repo/types`, wire sebagai dependency api+web. | Phase 2+ |
| BL-14 | **`auth.ts` masih 516 baris** (>400 limit §3.1). TASK-012 memecah `lms.ts`; `auth.ts` masih monolitik. | TASK-012 | 🟡 Medium | Pecah `auth.ts` ke `modules/auth/{register,login,oauth,password,session}.ts` dengan pola sama seperti lms. | Phase 2+ |
| BL-15 | **npm audit: 1 high + 1 moderate (Next.js 16.2.x + postcss)** tanpa forward-fix. **DECIDED: Accept + mitigate** (2 Jul 2026) — mitigasi edge Nginx + CSP + monitor patch mingguan; eskalasi ke fallback Next 15 jika eksploitabel. Detail: `docs/SECURITY_CHECKLIST.md`. | TASK-013 | 🔴 High | Monitor rilis patch Next 16. Edge rate-limit di TASK-020. Jangan `npm audit fix --force`. | Accepted — monitoring |
| BL-16 | **CSP belum nonce-based** — baseline CSP + HSTS ditambah (TASK-013) tapi pakai `'unsafe-inline'`/`'unsafe-eval'` (dibutuhkan Next/Tailwind tanpa nonce). | TASK-013 | 🟡 Medium | Implementasi nonce per-request (Next middleware) + hapus unsafe-inline dari script-src. | Phase 5 (TASK-051) |
| BL-11 | **Coverage modul kritis belum 80%** — ratchet gate sudah aktif di CI pada baseline (Stmts 60% · Branch 49% · Func 56% · Lines 62%). Target 80% dinaikkan bertahap per-modul (SSOT GAP-04). Per-file saat ini: `authenticate` 92% ✅, `authorize` ≥80% ✅, `orders` 75%, `webhooks` 76%, `couponService` 76%, `lms` 64%, `checkout` 64%, `coupons` 64%, `subscription` 53%, `auth` 48%, `reviews` 42%, `trainer` 37%, `affiliate` 35%. | TASK-010 | 🟡 Medium | Tambah integration test per modul secara bertahap; naikkan threshold vitest tiap kali coverage naik (jangan pernah turunkan). Prioritas revenue-path: orders→checkout→webhooks→lms→auth. | Phase 2+ (incremental) |
